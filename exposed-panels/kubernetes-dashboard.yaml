id: kubernetes-dashboard

info:
  name: Kubernetes Console Exposure
  author: Ice3man
  severity: low

requests:
  - method: GET
    path:
      - "{{BaseURL}}/"
    matchers:
      - type: word
        words:
          - "Kubernetes Dashboard</title>"