id: kafka-connect-ui

info:
  name: Apache Kafka Connect UI Exposure
  author: pd-team
  severity: low

requests:
  - method: GET
    path:
      - '{{BaseURL}}'

    matchers:
      - type: word
        words:
          - '<title>Kafka Connect UI</title>'