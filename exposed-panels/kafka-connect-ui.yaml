id: kafka-connect-ui

info:
  name: Apache Kafka Connect UI Exposure
  author: pdteam
  severity: low
  tags: panel,kafka

requests:
  - method: GET
    path:
      - '{{BaseURL}}'

    matchers:
      - type: word
        words:
          - '<title>Kafka Connect UI</title>'