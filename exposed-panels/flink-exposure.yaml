id: flink-exposure

info:
  name: Apache Flink Exposure
  author: pdteam
  severity: low
  tags: panel,apache,flink

requests:
  - method: GET
    path:
      - '{{BaseURL}}'

    matchers:
      - type: word
        words:
          - '<title>Apache Flink Web Dashboard</title>'
