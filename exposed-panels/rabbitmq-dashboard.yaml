id: rabbitmq-dashboard

info:
  name: RabbitMQ Dashboard
  author: fyoorer
  severity: info
  tags: panel,rabbitmq

requests:
  - method: GET
    path:
      - '{{BaseURL}}'
    matchers:
      - type: word
        words:
          - "RabbitMQ Management"
        part: body
