id: led-imediacloud-panel

info:
  name: LEDiMediaCloud Panel Detect
  author: ritikchaddha
  severity: info
  metadata:
    verified: true
    shodan-query: http.html:"LEDiMediaCloud"
  tags: led,mediacloud,panel

requests:
  - method: GET
    path:
      - "{{BaseURL}}/login"

    matchers-condition: and
    matchers:
      - type: word
        part: body
        words:
          - '<title>LEDiMediaCloud'

      - type: status
        status:
          - 200
