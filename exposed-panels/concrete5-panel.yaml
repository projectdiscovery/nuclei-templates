
id: concrete5-panel

info:
  name: Concrete5 Panel
  author: dhiyaneshDk
  severity: info
  metadata:
    shodan-query: http.title:"concrete5"
  tags: panel,concrete

requests:
  - method: GET
    path:
      - '{{BaseURL}}/index.php/login'

    matchers:
      - type: word
        words:
          - '<title>Login :: concrete5</title>'
