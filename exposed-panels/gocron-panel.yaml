id: gocron-panel

info:
  name: gocron panel
  author: ffffffff0x
  severity: info
  metadata:
    fofa-query: app="gocron-定时任务系统"
  tags: gocron,panel

requests:
  - method: GET
    path:
      - "{{BaseURL}}"

    redirects: true
    max-redirects: 2

    matchers:
      - type: word
        part: body
        words:
          - '<title>gocron - 定时任务系统</title>'
