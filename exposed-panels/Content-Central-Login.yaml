id: Content-Central-Login

info:
  name: Content Central Login
  author: theabhinavgaur
  severity: info
  tags: panel,Content Central

requests:
  - method: GET
    path:
      - "{{BaseURL}}/ContentCentral/login.aspx/"
      - "{{BaseURL}}/login.aspx"

    matchers-condition: and
    matchers:
      - type: word
        part: body
        words:
          - Content Central Login
        case-insensitive: true

      - type: status
        status:
          - 200
