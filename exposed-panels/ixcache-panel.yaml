id: ixcache-panel

info:
  name: iXCache pannel
  author: ffffffff0x
  severity: info
  metadata:
    fofa-query: app="iXCache"
  tags: ixcache,panel

requests:
  - method: GET
    path:
      - "{{BaseURL}}"

    redirects: true
    max-redirects: 2
    matchers-condition: and
    matchers:
      - type: word
        part: body
        words:
          - '<title>iXCache</title>'

      - type: status
        status:
          - 302
