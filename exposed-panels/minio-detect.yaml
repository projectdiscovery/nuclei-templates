id: minio-detect

info:
  name: Minio panel detect
  author: pikpikcu
  severity: info
  tags: panel

requests:
  - method: GET
    path:
      - "{{BaseURL}}/minio/login"
    matchers:
      - type: word
        words:
          - "<title>MinIO Browser</title>"
        part: body
