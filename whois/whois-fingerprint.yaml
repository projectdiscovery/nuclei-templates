id: whois-fingerprint

info:
  name: Whois (RDAP) query
  author: bru73f0rc3
  severity: info
  tags: whois

whois:
    - query: "{{Host}}"
      extractors:
         - type: json
           json:
             - '.Data | to_entries[]|[.key, .value]'