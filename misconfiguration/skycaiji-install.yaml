id: skycaiji-install

info:
  name: SkyCaiji Exposed Installation
  author: pikpikcu
  severity: high
  tags: tech,skycaiji,storage

requests:
  - method: GET
    path:
      - '{{BaseURL}}/index.php?s=/install/index/index'

    matchers-condition: and
    matchers:
      - type: word
        part: body
        words:
          - '<title>蓝天采集器 SkyCaiji 安装</title>'
          - 'https://www.skycaiji.com'
        condition: and

      - type: status
        status:
          - 200
