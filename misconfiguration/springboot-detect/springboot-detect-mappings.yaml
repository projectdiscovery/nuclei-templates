id: springboot-detect-mappings

info:
  name: Detect Springboot Mappings Actuator
  author: that_juan_ & dwisiswant0 & wdahlenb
  severity: medium
  description: Additional routes may be displayed

requests:
  - method: GET
    path:
      - "{{BaseURL}}/mappings"
      - "{{BaseURL}}/actuator/mappings"
    matchers-condition: and
    matchers:
      - type: word
        part: body
        words:
          - "mappings"
          - "method"
          - "produces"
        condition: and
      - type: status
        status:
          - 200
      - type: word
        words:
          - "application/json"
          - "application/vnd.spring-boot.actuator"
        condition: or
        part: header
