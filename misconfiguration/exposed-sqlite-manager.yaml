id: exposed-sqlite-manager

info:
  name: SQLiteManager
  author: dhiyaneshDK
  severity: medium
  reference: https://www.exploit-db.com/ghdb/5003

requests:
  - method: GET
    path:
      - '{{BaseURL}}/sqlite/'
      - '{{BaseURL}}/dev/SQLiteManager-1.2.4/'
      - '{{BaseURL}}/sqlitemanager/'

    matchers-condition: and
    matchers:
      - type: word
        words:
          - '<title>SQLiteManager</title>'
      - type: status
        status:
          - 200
