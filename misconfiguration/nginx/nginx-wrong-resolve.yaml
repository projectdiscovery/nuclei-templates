id: nginx-wrong-resolve

info:
  name: Nginx-Wrong-Resolve
  author: pwnhxl
  severity: high
  description: Nginx-Wrong-Resolve
  reference:
    - https://github.com/vulhub/vulhub/tree/master/nginx/nginx_parsing_vulnerability
  tags: misconfiguration,parsing,php

requests:
  - raw:
      - |
        GET /robots.txt HTTP/1.1
        Host: {{Hostname}}

      - |
        GET /robots.txt/{{randstr}}.php HTTP/1.1
        Host: {{Hostname}}

      - |
        GET /favicon.ico HTTP/1.1
        Host: {{Hostname}}

      - |
        GET /favicon.ico/{{randstr}}.php HTTP/1.1
        Host: {{Hostname}}

    stop-at-first-match: true
    matchers-condition: or
    matchers:
      - type: dsl
        dsl:
          - 'status_code_1 == 200 && status_code_2 == 200'
          - 'contains(all_headers_1, "text/plain")'
          - 'contains(all_headers_2, "text/html")'
          - 'contains(body_2, body_1)'
          - 'len(body_1) == len(body_2)'
        condition: and

      - type: dsl
        dsl:
          - 'status_code_3 == 200 && status_code_4 == 200'
          - 'contains(all_headers_3, "image/x-icon")'
          - 'contains(all_headers_4, "text/html")'
          - 'contains(body_3, body_4)'
          - 'len(body_3) == len(body_4)'
        condition: and