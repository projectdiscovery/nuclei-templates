id: ampache-music-installer

info:
  name: Ampache Musci Installer
  author: tess
  severity: high
  metadata:
    verified: true
    shodan-query: title:"For the Love of Music - Installation"
  tags: misconfig,ampache,exposure,installer

requests:
  - method: GET
    path:
      - '{{BaseURL}}/install.php'

    matchers-condition: and
    matchers:
      - type: word
        part: body
        words:
          - "<title>Ampache :: For the Love of Music - Installation</title>"
          - "Choose Installation Language</h1>"
        condition: and

      - type: word
        part: header
        words:
          - "text/html"

      - type: status
        status:
          - 200
