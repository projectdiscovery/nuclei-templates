id: filezilla-exposed

info:
  name: Filezilla
  author: amsda
  severity: medium
requests:
  - method: GET
    path:
      - "{{BaseURL}}/filezilla.xml"
      - "{{BaseURL}}/sitemanager.xml"
      - "{{BaseURL}}/FileZilla.xml"
    matchers:
      - type: word
        words:
          - "<FileZilla"
