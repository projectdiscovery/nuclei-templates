id: intercom-chat-detect

info:
  name: intercom
  author: tess
  severity: info

requests:
  - method: GET
    path:
      - "{{BaseURL}}"
    redirects: true
    max-redirects: 3  
    matchers:
      - type: word
        words:
          - 'intercom'
          
