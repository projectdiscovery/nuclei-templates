id: docker-public-repositories

info:
  name: Public Docker repositories
  author: nadino
  severity: critical

requests:
  - method: GET
    path:
      - "{{BaseURL}}/v2/_catalog"
    matchers:
      - type: word
        words:
            - 'repositories'
        part: body
