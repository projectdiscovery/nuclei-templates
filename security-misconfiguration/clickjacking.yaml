id: clickjacking

info:
  name: ClickJacking Detector
  author: kurohost
  severity: low

requests:
  - method: GET
    path:
      - "{{BaseURL}}"
    matchers:
      - type: dsl
        dsl:
          - "!contains(tolower(all_headers), 'x-frame-options')"
