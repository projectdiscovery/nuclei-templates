version: '3.8'
services:
  vulnerable-app:
    build:
      context: .
      dockerfile: Dockerfile.vulnerable
    ports:
      - "8080:80"
    volumes:
      - ./server/php/files:/var/www/html/files
