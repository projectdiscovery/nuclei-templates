id: laravel-detect

info:
  name: Laravel Detect
  author: pdteam
  severity: info

http:
  - method: GET
    path:
      - "{{BaseURL}}"

    matchers-condition: and
    matchers:
      - type: word
        words:
          - "laravel_session"
        part: header
