# Nuclei Configuration Profile for Deterministic CISA-KEV Triage (Dual-Signal Standard)
#
# Purpose:
# This profile prioritizes actively weaponized CISA Known Exploited Vulnerabilities (KEV)
# with verifiable evidence, filtering out passive version-banner alert noise.
#
# Operational Doctrine:
# - Enforces 'never_equate_scan_to_exploit: true'
# - Flags actionable 'fix_now' emergency items for immediate engineering change windows.
# - Suppresses theoretical or unweaponized version mismatches.
#
# Usage:
# nuclei -profile cisa-kev-triage -u https://target.example.com
# nuclei -profile cisa-kev-triage -l targets.txt -o verified_kev_findings.json -json-export

tags:
  - kev
  - cve
  - rce
  - auth-bypass
  - oob

severity:
  - critical
  - high
  - medium

metadata:
  triage_disposition: fix_now
  cisa_kev_active: true
