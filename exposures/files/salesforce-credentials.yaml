id: salesforce-credentials

info:
  name: Salesforce Information - Detect
  author: geeknik
  severity: unknown
  description: Salesforce credentials information was detected.
  reference:
    - https://github.com/daveagp/websheets
  tags: exposure,files,salesforce

requests:
  - method: GET
    path:
      - '{{BaseURL}}/js/salesforce.js'
      - '{{BaseURL}}/salesforce.js'

    stop-at-first-match: true
    matchers-condition: and
    matchers:
      - type: word
        words:
          - 'jsforce.Connection'
          - 'conn.login'
          - 'conn.query'
        condition: and

      - type: word
        part: header
        words:
          - "text/plain"

      - type: status
        status:
          - 200

# Enhanced by md on 2023/03/22
