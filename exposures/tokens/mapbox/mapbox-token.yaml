id: mapbox-api-token

info:
  name: Mapbox Token
  author: Devang-Solanki
  severity: medium
  tags: token,file,mapbox

requests:
  - method: GET
    path:
      - "{{BaseURL}}"
    extractors:
      - type: regex
        part: body
        regex:
          - 'sk\.eyJ1Ijoi\w+\.[\w-]*'