id: mapbox-api-token

info:
  name: Mapbox Token
  author: Devang-Solanki
  severity: medium
  tags: token,exposure,mapbox

requests:
  - method: GET
    path:
      - "{{BaseURL}}"
    extractors:
      - type: regex
        part: body
        regex:
          - '([a-z]+)\.eyJ1Ijoi\w+\.[\w-]*'
