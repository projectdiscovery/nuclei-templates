id: iam-mfa-enable
info:
  name: MFA not enabled for AWS IAM Console User
  author: princechaddha
  severity: high
  description: |
    Verifies that Multi-Factor Authentication (MFA) is enabled for all IAM users with console access in AWS
  reference:
    - https://docs.aws.amazon.com/cli/latest/reference/iam/list-mfa-devices.html
  tags: cloud,devops,aws,amazon,iam

flow: |
  code(1)
  for(let UserName of iterate(template.users)){
    set("user", UserName)
    code(2)
  }

self-contained: true
code:
  - engine:
      - sh
      - bash
    source: |
      aws iam list-users --query 'Users[*].UserName'

    extractors:
      - type: json # type of the extractor
        internal: true
        name: users
        json:
          - '.[]'

  - engine:
      - sh
      - bash
    source: |
         aws iam list-mfa-devices --user-name $user --query 'MFADevices'

    matchers:
      - type: word
        words:
          - "[]"

    extractors:
      - type: dsl
        dsl:
          - '"MFA is no enabled for IAM User " + user'
# digest: 490a0046304402207552f24af034526feff5a9de377bf130d0c69ea451012b5435daaeb44d0d3b950220724f367b6afb2d24f8c940fa7a493111debf3f5ab90c9b9943d760636d03eee6:366f2a24c8eb519f6968bd8801c08ebe