id: acrolinx-workflow

info:
  name: Acrolinx Security Checks
  author: daffainfo
  description: A simple workflow that runs all Acrolinx related nuclei templates on a given target.

workflows:
  - template: exposed-panels/acrolinx-dashboard.yaml
    subtemplates:
      - tags: acrolinx
