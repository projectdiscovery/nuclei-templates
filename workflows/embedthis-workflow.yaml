id: embedthis-workflow

info:
  name: Embedthis Security Checks
  author: daffainfo
  description: A simple workflow that runs all Embedthis related nuclei templates on a given target.

workflows:
  - template: technologies/fingerprinthub-web-fingerprints.yaml
    subtemplates:
      - tags: embedthis
