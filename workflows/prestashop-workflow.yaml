id: prestashop-workflow

info:
  name: Prestashop Security Checks
  author: pastaga
  description: Detect exposed Prestashop panel, then run all related nuclei templates.
workflows:
  - template: http/exposed-panels/prestashop-admin-panel.yaml
    subtemplates:
      - tags: prestashop
