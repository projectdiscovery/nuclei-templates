id: fortinet-workflow

info:
  name: Fortinet Security Checks
  author: daffainfo
  description: A simple workflow that runs all fortinet related nuclei templates on a given target.

workflows:
  - template: exposed-panels/fortinet/fortinet-panel.yaml
    subtemplates:
      - tags: fortinet
