id: rancher-workflow

info:
  name: Rancher Security Checks
  author: daffainfo
  description: A simple workflow that runs all Rancher related nuclei templates on a given target.

workflows:
  - template: exposed-panels/rancher-panel.yaml
    subtemplates:
      - template: default-logins/rancher/rancher-default-login.yaml
