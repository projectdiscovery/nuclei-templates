id: gogs-workflow

info:
  name: Gogs (Go Git Service) - Security Checks
  author: daffainfo
  description: A simple workflow that runs all Gogs related nuclei templates on a given target.

workflows:
  - template: exposed-panels/gogs-panel.yaml
    subtemplates:
      - tags: gogs
