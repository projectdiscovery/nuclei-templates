id: robot-txt-endpoint-workflow

info:
  name: extract endpoints from robots.txt
  author: CasperGN

variables:
  robots: files/robots.txt.yaml
  extract_endpoints: files/robots-endpoints.yaml

logic:
  |
  if robots() {
    extract_endpoints()
  }