id: ricoh-workflow

info:
  name: Ricoh Security Checks
  author: daffainfo
  description: A simple workflow that runs all Ricoh related nuclei templates on a given target.

workflows:
  - template: technologies/favicon-detection.yaml
    subtemplates:
      - template: default-logins/ricoh/ricoh-weak-password.yaml
