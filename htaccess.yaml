id: htaccess

info:
  name: htaccess file
  author: Yash Anand @yashanand155
  severity: info

requests:
  - method: GET
    path:
      - "{{BaseURL}}/.htaccess"
    matchers:
      - type: word
        words:
          - "RewriteRule"
