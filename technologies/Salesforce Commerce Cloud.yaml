id: Salesforce Commerce Cloud

info:
  name: Salesforce Commerce Cloud
  author: hakluke
  severity: low

requests:
  - method: GET
    path:
      - "{{BaseURL}}/"
    matchers:
      - type: regex
        part: body
        regex:
          - "<[^>]+demandware\\.edgesuite"