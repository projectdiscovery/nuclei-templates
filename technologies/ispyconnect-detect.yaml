id: ispyconnect-detect

info:
  name: iSpyConnect - Detect
  author: arafatansari
  severity: info
  metadata:
    shodan-query: http.html:"iSpy"
    verified: true
  tags: ispy,tech

requests:
  - method: GET
    path:
      - '{{BaseURL}}'

    matchers-condition: and
    matchers:
      - type: word
        part: body
        words:
          - 'iSpy is running'
        condition: and

      - type: word
        part: header
        words:
          - 'Server: iSpy'

      - type: status
        status:
          - 200
