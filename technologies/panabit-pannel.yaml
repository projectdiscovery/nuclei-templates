id: panabit-pannel

info:
  name: panabit pannel
  author: ffffffff0x
  severity: info
  metadata:
    fofa-query: app="Panabit-智能网关"
  tags: panabit,pannel

requests:
  - method: GET
    path:
      - "{{BaseURL}}/login/login.htm"

    matchers-condition: and
    matchers:
      - type: word
        words:
          - '<title>Panabit</title>'
        part: body
        condition: and

      - type: status
        status:
          - 200
