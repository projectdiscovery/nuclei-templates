id: icecast-server-detect

info:
  name: Icecast Streaming Media Server
  author: theamanrawat
  severity: info
  metadata:
    verified: true
    shodan-query: http.title:"Icecast Streaming Media Server"
  tags: tech, icecast-server

requests:
  - method: GET
    path:
      - '{{BaseURL}}'

    matchers-condition: and
    matchers:
      - type: word
        part: body
        words:
          - '<title>Icecast Streaming Media Server</title>'

      - type: word
        part: header
        words:
          - "Icecast"

      - type: status
        status:
          - 200