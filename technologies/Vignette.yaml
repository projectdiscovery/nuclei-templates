id: Vignette

info:
  name: Vignette
  author: hakluke
  severity: low

requests:
  - method: GET
    path:
      - "{{BaseURL}}/"
    matchers:
      - type: regex
        part: body
        regex:
          - "<[^>]+=\"vgn-?ext"