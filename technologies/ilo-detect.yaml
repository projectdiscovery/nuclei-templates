id: ilo-detect

info:
  name: HP iLO
  author: milo2012
  severity: info
  description: Version of HP iLO

requests:
  - method: GET
    path:
      - "{{BaseURL}}/xmldata?item=all"

    extractors:
      - type: regex
        part: body
        regex:
          - "<FWRI>(.*)</FWRI>"

