id: piwigo-detect

info:
  name: Piwigo Detect
  author: pikpikcu
  severity: info
  metadata:
    fofa-query: title="piwigo"
  tags: piwigo,tech

requests:
  - method: GET
    path:
      - "{{BaseURL}}"

    matchers-condition: and
    matchers:
      - type: word
        part: body
        words:
          - '<a href="https://cn.piwigo.org">Piwigo</a>'

      - type: status
        status:
          - 200
