id: drupal-detector

info:
  name: Drupal Detection
  author: 1nf1n7y
  severity: info
  metadata:
    verified: true
    shodan-query: http.component:"Drupal"
  tags: tech,drupal

requests:
  - method: GET
    path:
      - "{{BaseURL}}/CHANGELOG.txt"

    matchers-condition: and
    matchers:
      - type: word
        part: body
        words:
          - 'Initial release'
          - 'Drupal 1.0.0'
        condition: and

      - type: status
        status:
          - 200
