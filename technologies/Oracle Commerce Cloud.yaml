id: Oracle Commerce Cloud

info:
  name: Oracle Commerce Cloud
  author: hakluke
  severity: low

requests:
  - method: GET
    path:
      - "{{BaseURL}}/"
    matchers:
      - type: regex
        part: body
        regex:
          - "<[^>]+id=\"oracle-cc\""