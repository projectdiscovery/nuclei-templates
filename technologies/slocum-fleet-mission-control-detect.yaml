id: slocum-fleet-mission-control
info:
  name: slocum fleet mission control
  author: pussycat0x
  severity: info
  tags: tech
requests:
  - method: GET
    path:
      - "{{BaseURL}}/sfmc/login"
      - "{{BaseURL}}/"
    matchers-condition: and
    matchers:
      - type: word
        words:
          - "<title>Slocum Fleet Mission Control - Login</title>"
      - type: status
        status:
          - 200