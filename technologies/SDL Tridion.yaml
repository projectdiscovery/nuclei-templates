id: SDL Tridion

info:
  name: SDL Tridion
  author: hakluke
  severity: low

requests:
  - method: GET
    path:
      - "{{BaseURL}}/"
    matchers:
      - type: regex
        part: body
        regex:
          - "<img[^>]+_tcm\\d{2,3}-\\d{6}\\."