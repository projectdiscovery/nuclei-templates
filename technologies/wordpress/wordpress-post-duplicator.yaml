id: wordpress-post-duplicator

info:
  name: "Post Duplicator Detection"
  author: ricardomaia
  severity: info
  reference:
    - https://wordpress.org/plugins/post-duplicator/
  metadata:
    plugin_namespace: post-duplicator
    wpscan: https://wpscan.com/plugin/post-duplicator
  tags: tech,wordpress,plugin,top-200

requests:
  - method: GET
    redirects: true
    max-redirects: 2
    path:
      - "{{BaseURL}}/wp-content/plugins/post-duplicator/readme.txt"
    matchers-condition: and
    matchers:
      - type: regex
        regex:
          - "(?i)Stable.Tag"
      - type: status
        status:
          - 200
    extractors:
      - type: regex
        name: version
        part: body
        group: 1
        regex:
          - '(?i)Stable.tag:\s?([\w.]+)'
