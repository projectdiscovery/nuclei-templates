id: Store Systems

info:
  name: Store Systems
  author: hakluke
  severity: low

requests:
  - method: GET
    path:
      - "{{BaseURL}}/"
    matchers:
      - type: regex
        part: body
        regex:
          - "Shopsystem von <a href=[^>]+store-systems\\.de\"|\\.mws_boxTop"