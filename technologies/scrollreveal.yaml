id: scrollreveal

info:
  name: scrollreveal
  author: hakluke
  severity: low

requests:
  - method: GET
    path:
      - "{{BaseURL}}/"
    matchers:
      - type: regex
        part: body
        regex:
          - "<[^>]+data-sr(?:-id)"