id: DovetailWRP

info:
  name: DovetailWRP
  author: hakluke
  severity: low

requests:
  - method: GET
    path:
      - "{{BaseURL}}/"
    matchers:
      - type: regex
        part: body
        regex:
          - "<link[^>]* href=\"\\/DovetailWRP\\/"