id: Combeenation

info:
  name: Combeenation
  author: hakluke
  severity: low

requests:
  - method: GET
    path:
      - "{{BaseURL}}/"
    matchers:
      - type: regex
        part: body
        regex:
          - "<iframe[^>]+src=\"[^>]+portal\\.combeenation\\.com"