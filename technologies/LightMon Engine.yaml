id: LightMon Engine

info:
  name: LightMon Engine
  author: hakluke
  severity: low

requests:
  - method: GET
    path:
      - "{{BaseURL}}/"
    matchers:
      - type: regex
        part: body
        regex:
          - "<!-- Lightmon Engine Copyright Lightmon"