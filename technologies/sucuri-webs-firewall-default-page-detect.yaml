id: sucuri-webs-firewall-default-page-detect
info:
  name: Sucuri Website Firewall - Not Configured
  author: pussycat0x
  severity: info
  metadata:
    fofa-dork: "sucuri firewall"
  tags: tech, default 
requests:
  - method: GET
    path:
      - "{{BaseURL}}/"
    matchers-condition: and
    matchers:
      - type: word
        words:
          - '<title>Sucuri WebSite Firewall - Not Configured</title>'
        condition: and  
      - type: status
        status:
          - 404