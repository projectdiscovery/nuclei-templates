id: generic-webserver-version-detect
info:
  name: Detect Apache Version
  author: philippedelteil
  description: Obtain generic webserver versions
  severity: info
requests:
  - method: GET
    path:
      - "{{BaseURL}}"
    extractors:
      - type: kval
        part: header
        kval:
          - Server
