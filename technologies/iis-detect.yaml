id: iis-version-detect
info:
  name: IIS Version
  author: philippedelteil
  description: Some IIS servers have the version on the response header.
  severity: info
requests:
  - method: GET
    path:
      - "{{BaseURL}}"
    extractors:
      - type: regex
        part: header
        regex:
          - "IIS.+"
