id: home-assistant

info:
  name: Detect Home Assistant
  author: fabaff
  severity: info
  tags: tech,iot

requests:
  - method: GET
    path:
      - "{{BaseURL}}"
    matchers:
      - type: word
        words:
          - "<title>Home Assistant</title>"
