id: hanwang-detect

info:
  name: hanwang-detect
  author: ffffffff0x
  severity: info
  metadata:
    fofa-query: "汉王人脸考勤管理系统"
  tags: hanwang,detect

requests:
  - method: GET
    path:
      - "{{BaseURL}}"

    redirects: true
    max-redirects: 2
    matchers-condition: and
    matchers:
      - type: word
        part: body
        words:
          - '汉王人脸考勤管理系统'


      - type: status
        status:
          - 200