id: oracle-webcenter-sites

info:
  name: Oracle WebCenter Sites
  author: leovalcante
  severity: info
  tags: tech,oracle,wcs

requests:
  - method: GET
    path:
      - '{{BaseURL}}/cs/Satellite'

    matchers-condition: and
    matchers:
      - type: word
        words:
          - 'Content Server could not process the request'

      - type: status
        status:
          - 400

