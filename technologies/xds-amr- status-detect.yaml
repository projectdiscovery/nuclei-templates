id: xds-amr-status
info:
  name: XDS-AMR - status
  author: pussycat0x
  severity: info
  metadata:
    shodan-dork: 'http.title:"XDS-AMR - status"'
  tags: login,tech
requests:
  - method: GET
    path:
      - "{{BaseURL}}/"
      - "{{BaseURL}}/login.php"
    matchers-condition: and
    matchers:
      - type: word
        words:
          - '<title>XDS-AMR - Status</title>'
        condition: and
        part: body
      - type: status
        status:
          - 200