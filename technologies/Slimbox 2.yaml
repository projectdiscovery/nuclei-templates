id: Slimbox 2

info:
  name: Slimbox 2
  author: hakluke
  severity: low

requests:
  - method: GET
    path:
      - "{{BaseURL}}/"
    matchers:
      - type: regex
        part: body
        regex:
          - "<link [^>]*href=\"[^/]*slimbox2(?:-rtl)?\\.css"