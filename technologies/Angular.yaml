id: Angular

info:
  name: Angular
  author: hakluke
  severity: low

requests:
  - method: GET
    path:
      - "{{BaseURL}}/"
    matchers:
      - type: regex
        part: body
        regex:
          - "<[^>]+ ng-version=\"([\\d.]+)\""