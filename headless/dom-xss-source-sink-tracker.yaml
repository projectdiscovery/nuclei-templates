id: dom-xss-source-sink-tracker

info:
  name: DOM XSS Source-to-Sink Tracker
  author: geeknik
  severity: medium
  description: |
    Detects potential DOM XSS by tracing a randomized marker from URL and postMessage
    sources into dangerous DOM and JavaScript sinks.
  metadata:
    max-request: 1
    verified: true
  tags: headless,xss,domxss,postmessage,vuln

variables:
  token: "{{randstr_1}}"

headless:
  - steps:
      - action: setheader
        args:
          part: response
          key: Content-Security-Policy
          value: "default-src * 'unsafe-inline' 'unsafe-eval' data: blob:;"

      - action: script
        args:
          hook: true
          code: |
            () => {
              const token = "{{token}}";
              window.__nucleiDomXssFindings = [];

              const log = (sink, value) => {
                try {
                  const content = String(value);
                  if (content.includes(token)) {
                    window.__nucleiDomXssFindings.push({
                      sink: sink,
                      value: content.slice(0, 300)
                    });
                  }
                } catch (e) {}
              };

              const hookSetter = (proto, property, sinkName) => {
                try {
                  const descriptor = Object.getOwnPropertyDescriptor(proto, property);
                  if (!descriptor || !descriptor.set) {
                    return;
                  }
                  Object.defineProperty(proto, property, {
                    configurable: true,
                    enumerable: descriptor.enumerable,
                    get: descriptor.get,
                    set: function(v) {
                      log(sinkName, v);
                      return descriptor.set.call(this, v);
                    }
                  });
                } catch (e) {}
              };

              hookSetter(Element.prototype, "innerHTML", "Element.innerHTML");
              hookSetter(Element.prototype, "outerHTML", "Element.outerHTML");

              const oldInsertAdjacentHTML = Element.prototype.insertAdjacentHTML;
              if (oldInsertAdjacentHTML) {
                Element.prototype.insertAdjacentHTML = function(position, html) {
                  log("Element.insertAdjacentHTML", html);
                  return oldInsertAdjacentHTML.apply(this, arguments);
                };
              }

              const oldDocumentWrite = document.write;
              document.write = function(content) {
                log("document.write", content);
                return oldDocumentWrite.apply(document, arguments);
              };

              const oldEval = window.eval;
              window.eval = function(content) {
                log("eval", content);
                return oldEval.apply(window, arguments);
              };

              const oldSetTimeout = window.setTimeout;
              window.setTimeout = function(handler) {
                if (typeof handler === "string") {
                  log("setTimeout(string)", handler);
                }
                return oldSetTimeout.apply(window, arguments);
              };

              const oldSetInterval = window.setInterval;
              window.setInterval = function(handler) {
                if (typeof handler === "string") {
                  log("setInterval(string)", handler);
                }
                return oldSetInterval.apply(window, arguments);
              };

              const OldFunction = window.Function;
              window.Function = function() {
                const args = Array.from(arguments);
                log("Function", args.join(","));
                return OldFunction.apply(this, args);
              };
              window.Function.prototype = OldFunction.prototype;

              window.__nucleiDomXssToken = token;
            }

      - action: navigate
        args:
          url: "{{BaseURL}}/?nuclei_dom={{token}}#{{token}}"

      - action: waitload

      - action: waitdom

      - action: script
        args:
          code: |
            () => {
              const token = window.__nucleiDomXssToken || "{{token}}";
              try {
                window.postMessage(token, "*");
              } catch (e) {}
              try {
                window.postMessage({token: token, payload: token}, "*");
              } catch (e) {}
              try {
                window.dispatchEvent(new MessageEvent("message", {data: token, origin: window.origin}));
              } catch (e) {}
              return "sent";
            }

      - action: sleep
        args:
          duration: 2

      - action: script
        name: findings
        args:
          code: |
            () => JSON.stringify(window.__nucleiDomXssFindings || [])

    matchers-condition: and
    matchers:
      - type: word
        part: findings
        words:
          - '"sink":'

      - type: word
        part: findings
        words:
          - "{{token}}"

    extractors:
      - type: kval
        part: findings
        kval:
          - findings
