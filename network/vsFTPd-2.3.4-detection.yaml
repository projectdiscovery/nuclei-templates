id: vsFTPd 2.34 Detection

info:
  name: vsFTPd Detection
  author: pussycat0x
  severity: Critical
  tags: network,openssh,vsftpd,ftp
  description: vsFTPd Version Detecting
  reference: https://www.rapid7.com/db/modules/exploit/unix/ftp/vsftpd_234_backdoor/

network:
  - inputs:
      - data: "USER anonymous\r\nPASS pussycat0x\r\n"

    host:
      - "{{Hostname}}"
      - "{{Hostname}}:21"

    matchers:
      - type: word
        words:
          - "vsFTPd 2.3.4"
