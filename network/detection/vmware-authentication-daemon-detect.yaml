id: vmware-authentication-daemon

info:
  name: VMware Authentication Daemon Detection
  author: pussycat0x
  severity: info
  description: |
    vmauthd is the VMWare authentication daemon that is included with many VMWare products, including ESX(i), and Workstation.
  metadata:
    verified: true
    max-request: 1
    shodan-query: product:"VMware Authentication Daemon"
  tags: network,vmware,authenticated,detect,detection,tcp,discovery
tcp:

  - inputs:
      - data: "\n"
    host:
      - "{{Hostname}}"
    port: 902

    matchers:
      - type: word
        words:
          - "ServerDaemonProtocol:SOAP"
          - "MKSDisplayProtocol:VNC"
        condition: and

    extractors:
      - type: regex
        regex:
          - "VMware Authentication Daemon Version ([0-9.]+)"
# digest: 4a0a00473045022100a27696da204430fbc4892cfde7458cb958b5d8de1f47596f5a4b756fcf22d21b02200a244de98ab0b0a00d4acf8610322c1d0f4298fe502f505487ce4250dc0c7726:922c64590222798bb761d5b6d8e72950