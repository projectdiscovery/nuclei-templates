id: tornado-vxworks-ftp-detect
info:
  name: Tornado-VxWorks FTP Service - Detect
  author: pussycat0x
  severity: info
  description: Tornado-VxWorks FTP service was detected.
  metadata:
    verified: true
    shodan-query: "220 Tornado-vxWorks"
    max-request: 1
  tags: network,ftp,vxworks,detect,tcp

tcp:
  - inputs:
      - data: 00000000
        type: hex

    host:
      - "{{Hostname}}"
    port: 21
    read-size: 1024

    matchers:
      - type: word
        words:
          - "Tornado-vxWorks (VxWorks"

    extractors:
      - type: regex
        group: 1
        name: version
        regex:
          - "VxWorks([0-9.]+)"