id: zywall-ftp-detect
info:
  name: ZyWALL FTP Service - Detect
  author: pussycat0x
  severity: info
  description: ZyWALL FTP service was detected.
  metadata:
    verified: true
    shodan-query: "FTP Server (ZyWALL"
    max-request: 1
  tags: network,ftp,zywall,detect,firewall,tcp

tcp:
  - inputs:
      - data: 00000000
        type: hex

    host:
      - "{{Hostname}}"
    port: 21
    read-size: 1024

    matchers:
      - type: word
        words:
          - "FTP Server (ZyWALL"