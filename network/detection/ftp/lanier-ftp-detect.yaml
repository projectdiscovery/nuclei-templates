id: lanier-ftp-detect
info:
  name: LANIER MP 2555 FTP Service - Detect
  author: pussycat0x
  severity: info
  description: LANIER MP 2555 FTP service was detected.
  metadata:
    verified: true
    shodan-query: "LANIER MP 2555 FTP server"
    max-request: 1
  tags: network,ftp,lanier,detect,tcp

tcp:
  - inputs:
      - data: 00000000
        type: hex

    host:
      - "{{Hostname}}"
    port: 21
    read-size: 1024

    matchers:
      - type: word
        words:
          - "LANIER MP 2555 FTP server"

    extractors:
      - type: regex
        group: 1
        name: version
        regex:
          - "server ([(0-9.)]+)"