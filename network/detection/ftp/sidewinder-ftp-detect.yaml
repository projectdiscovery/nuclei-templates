id: sidewinder-ftp-detect
info:
  name: Sidewinder FTP Proxy Service - Detect
  author: pussycat0x
  severity: info
  description: Sidewinder FTP Proxy service was detected.
  metadata:
    verified: true
    shodan-query: "Sidewinder ftp proxy"
    max-request: 1
  tags: network,ftp,sidewinder,detect,tcp

tcp:
  - inputs:
      - data: 00000000
        type: hex

    host:
      - "{{Hostname}}"
    port: 21
    read-size: 1024

    matchers:
      - type: word
        words:
          - "Sidewinder ftp proxy"