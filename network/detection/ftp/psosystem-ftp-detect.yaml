id: psosystem-ftp-detect
info:
  name: pSOSystem FTP Service - Detect
  author: pussycat0x
  severity: info
  description: pSOSystem FTP service was detected.
  metadata:
    shodan-query: "pSOSystem FTP server"
    max-request: 1
  tags: network,ftp,psosystem,detect,tcp

tcp:
  - inputs:
      - data: 00000000
        type: hex

    host:
      - "{{Hostname}}"
    port: 21
    read-size: 1024

    matchers:
      - type: word
        words:
          - "pSOSystem FTP server"