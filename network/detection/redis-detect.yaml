id: redis-detect

info:
  name: Redis Service - Detect
  author: pussycat0x
  severity: info
  description: Redis service was detected.
  classification:
    cvss-metrics: CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:N/I:N/A:N
    cwe-id: CWE-200
  metadata:
    verified: true
    max-request: 2
    shodan-query:
      - product:"redis"
      - http.title:"airflow - dags" || http.html:"apache airflow"
      - http.title:"sign in - airflow"
    product: airflow
    vendor: apache
    fofa-query:
      - apache airflow
      - title="airflow - dags" || http.html:"apache airflow"
      - title="sign in - airflow"
    google-query:
      - intitle:"airflow - dags" || http.html:"apache airflow"
      - intitle:"sign in - airflow"
  tags: network,redis,detect,detection,tcp
tcp:
  - inputs:
      - data: "*1\r\n$4\r\ninfo\r\n"

    host:
      - "{{Hostname}}"
      - "tls://{{Hostname}}"
    port: 6379,6380
    read-size: 1024

    matchers:
      - type: word
        words:
          - "DENIED Redis"
          - "CONFIG REWRITE"
          - "NOAUTH Authentication"
        condition: or
# digest: 4a0a004730450220583c25f4677dd67ebf2a4d81cad7bd45ec70b41ff35cc143de2f62759639af56022100ab32c62800ce2dc0f788cac1c8d1dc4a5c8ba63959f5c3f6cd47b84a782b9cd8:922c64590222798bb761d5b6d8e72950