id: instagram-phish

info:
  name: instagram Phishing Detection
  author: rxerium
  severity: info
  description: |
    A instagram Phishing website was detected
  reference:
    - https://instagram.com
  tags: phishing,instagram

http:
  - method: GET
    path:
      - "{{BaseURL}}"

    host-redirects: true
    max-redirects: 2

    matchers-condition: and
    matchers:
      - type: word
        words:
          - 'Login • Instagram'

      - type: status
        status:
          - 200

      - type: dsl
        dsl:
          - '!contains(host,"instagram.com")'