id: apache-hive-detect

info:
  name: Apache Hive - Detect
  author: icarot
  severity: info
  description: |
   Detects a Apache Hive web application, a data warehouse software facilitates reading, writing, and managing large datasets residing in distributed storage using SQL.
  reference:
    - 'https://github.com/apache/hive'
  classification:
    cpe: cpe:2.3:a:apache:hive:4.6.0:*:*:*:*:*:*:*
  metadata:
    max-request: 1
    vendor: apache
    product: hive
    shodan-query: title:"HiveServer2"
  tags: apache,hive,tech

http:
  - method: GET
    path:
      - "{{BaseURL}}"

    matchers-condition: and
    matchers:
      - type: word
        part: body
        words:
          - '<title>HiveServer2</title>'
          - 'hiveserver2'

      - type: status
        status:
          - 200
    extractors:
      - type: regex
        name: version
        group: 1
        part: body
        regex:
          - "<td>([0-9]{1,3}\\.[0-9]{1,3}\\.[0-9]{1,3}), \\S+</td>\\n\\s+<td>Hive version and revision</td>"
