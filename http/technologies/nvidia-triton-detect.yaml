id: nvidia-triton-detect
info:
  name: NVIDIA Triton Inference Server - Detect
  author: mailler0xa
  severity: info
  description: |
    Detects the NVIDIA Triton Inference Server, an open-source platform for serving AI/ML models.Supports multiple frameworks and exposes HTTP/gRPC endpoints for high-performance inferencing.
  reference:
    - https://github.com/triton-inference-server/server
  metadata:
    verified: true
    max-request: 1
    vendor: NVIDIA
    product: Triton Inference Server
  tags: oss,nvidia,triton,tech

http:
  - method: GET
    path:
      - "{{BaseURL}}/v2"

    matchers-condition: and
    matchers:
      - type: word
        words:
          - "{\"name\":\"triton\","
          - "\"extensions\""
        condition: and

      - type: status
        status:
          - 200

    extractors:
      - type: json
        part: body
        json:
          - ".version"