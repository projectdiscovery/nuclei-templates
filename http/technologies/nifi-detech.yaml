id: apache-nifi-detect

info:
  name: Apache NiFi detect
  author: dwisiswant0
  severity: info
  metadata:
    max-request: 7
  tags: tech,apache,nifi

http:
  - method: GET
    path:
      - "{{BaseURL}}/access/config"
      - "{{BaseURL}}/controller/config"
      - "{{BaseURL}}/controller/registry-clients"
      - "{{BaseURL}}/counters"
      - "{{BaseURL}}/flow/registries"
      - "{{BaseURL}}/system-diagnostics"
      - "{{BaseURL}}/nifi-api/access/config"

    stop-at-first-match: true

    matchers-condition: and
    matchers:
      - type: regex
        part: body
        regex:
          - "supportsLogin"
          - "disconnectedNodeAcknowledged"
          - "(aggregate|node)Snapshots?"
        condition: or

      - type: status
        status:
          - 200
# digest: 4a0a00473045022100e640db8f0285eb013db1f5960f7397880cc1e20a43cdc11257f271c232a2b2e2022060d8b776963fa146607eacb73cd6d8ae595bbed927547101d7bdf97c1d5f18ee:922c64590222798bb761d5b6d8e72950