id: wordpress-wp-optimize

info:
  name: WP-Optimize – Cache, Compress images, Minify & Clean database to boost page speed & performance Detection
  author: ricardomaia
  severity: info
  reference:
    - https://wordpress.org/plugins/wp-optimize/
  metadata:
    plugin_namespace: wp-optimize
    wpscan: https://wpscan.com/plugin/wp-optimize
  tags: tech,wordpress,wp-plugin,top-100,top-200

http:
  - method: GET
    path:
      - "{{BaseURL}}/wp-content/plugins/wp-optimize/readme.txt"

    payloads:
      last_version: helpers/wordpress/plugins/wp-optimize.txt

    extractors:
      - type: regex
        part: body
        internal: true
        name: internal_detected_version
        group: 1
        regex:
          - '(?i)Stable.tag:\s?([\w.]+)'

      - type: regex
        part: body
        name: detected_version
        group: 1
        regex:
          - '(?i)Stable.tag:\s?([\w.]+)'

    matchers-condition: or
    matchers:
      - type: dsl
        name: "outdated_version"
        dsl:
          - compare_versions(internal_detected_version, concat("< ", last_version))

      - type: regex
        part: body
        regex:
          - '(?i)Stable.tag:\s?([\w.]+)'
# digest: 480a00453043021f1d5c30bf2c3fe50e8febcfd5e58ab87514f3a208b0a5223c215057ae526a7402203ada6cd162635cd9668e6acc1d55e7a2b9950187b38add28a7bd472a1fecd04d:922c64590222798bb761d5b6d8e72950