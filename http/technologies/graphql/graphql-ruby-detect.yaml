id: graphql-ruby-detect
info:
  name: Graphql Ruby Detect
  author: princechaddha
  severity: info
  reference: https://github.com/dolevf/graphw00f/blob/main/graphw00f/lib.py
  tags: tech,graphql,ruby,discovery

http:
  - method: POST
    path:
      - "{{BaseURL}}/graphql"
      - "{{BaseURL}}/api/graphql"
      - "{{BaseURL}}/query"
      - "{{BaseURL}}/"
    body: '{"query":"query @skip {__typename}"}'
    headers:
      Content-Type: application/json
    redirects: true

    matchers-condition: and
    matchers:
      - type: status
        status:
          - 200
          - 400

      - type: word
        words:
          - "'@skip' can't be applied to queries (allowed: fields, fragment spreads, inline fragments)"
        part: body
# digest: 490a004630440220624b16ac9af3c7487fc6fec1c7a2e3400881a512ac0249baaca87c2e58e36ee802206b5006e78a4685c7d5eb679936347223fa4a7fd1204b00d631a3482ced1b334b:922c64590222798bb761d5b6d8e72950