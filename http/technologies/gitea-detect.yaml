id: gitea-detect

info:
  name: Gitea Detect
  author: pikpikcu,righettod
  severity: info
  description: Gitea was detected.
  reference:
    - https://about.gitea.com/
    - https://docs.gitea.com/api
  metadata:
    verified: true
    max-request: 1
    shodan-query: http.title:"Gitea"
  tags: tech,gitea

http:
  - method: GET
    path:
      - "{{BaseURL}}/user/login"

    matchers-condition: and
    matchers:
      - type: word
        part: body
        words:
          - "Powered by Gitea"
          - 'content="Gitea'
        condition: or

      - type: status
        status:
          - 200

    extractors:
      - type: regex
        part: body
        group: 1
        regex:
          - "Powered by Gitea(\\s*)Version:(\\s*)([\\d.]+)"
          - "Gitea Version: ([0-9a-z+-.~]+)"
          - 'theme-gitea-auto.css\?v=([0-9a-z+-.~]+)'
          - 'encodeURIComponent.{2}([0-9a-z+-.~]+).{2}'
# digest: 4a0a004730450220440cddf7f94e78dcd10f8bebe120d158f561de91953a3159d88083a45b81e366022100b4bf89555e10fb4bd79c8aa6c6c78ecc99da57a40cd5ea64eb167d3d1178bcaa:922c64590222798bb761d5b6d8e72950