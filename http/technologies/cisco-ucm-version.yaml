id: cisco-ucm-version

info:
  name: Cisco Unified Communications Manager - Version Extraction
  author: Morgan Robertson
  severity: info
  description: Extracts the version number of Cisco Unified Communications Manager from the UDS API.
  reference:
    - https://developer.cisco.com/site/user-data-services/develop-and-test/api-reference/#version
  metadata:
    product: unified_communications_manager
    vendor: cisco
    shodan-query: http.title:"Unified Communications Self Care Portal"
    fofa-query: title="Unified Communications Self Care Portal"
  tags: cisco,ucm,version,api

http:
  - method: GET
    path:
      - "{{BaseURL}}/cucm-uds/version"

    headers:
      Accept: application/xml

    matchers-condition: and
    matchers:
      - type: status
        status:
          - 200

      - type: word
        part: body
        words:
          - '<versionInformation'
          - '<version>'
        condition: and

    extractors:
      - type: xpath
        name: version
        xpath:
          - "/versionInformation/version"