id: devexpress-detect
info:
  name: DevExpress Detect
  author: CravateRouge
  severity: info

http:
  - method: GET
    path:
      - "{{BaseURL}}"
    redirects: true
    matchers:
      - type: word
        words:
          - "DXR.axd"
        part: body
