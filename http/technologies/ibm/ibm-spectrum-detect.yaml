id: ibm-spectrum-detect

info:
  name: IBM Spectrum - Detect
  author: righettod
  severity: info
  description: |
    IBM Spectrum products was detected.
  reference:
    - https://www.ibm.com/docs/en/products?filter=spectrum
  metadata:
    max-request: 1
    verified: true
    shodan-query: http.title:"IBM Spectrum"
  tags: tech,ibm,spectrum

http:
  - method: GET
    path:
      - "{{BaseURL}}/"
      - "{{BaseURL}}/BACLIENT"
      - "{{BaseURL}}/JNLP"

    stop-at-first-match: true
    matchers:
      - type: dsl
        dsl:
          - 'status_code == 200'
          - 'contains_all(to_lower(body), "ibm spectrum", "com.ibm.")'
        condition: and

    extractors:
      - type: regex
        part: body
        group: 1
        regex:
          - '(?i)"guiVersion"\s*:\s*"([0-9.\-]+)"'
          - '(?i)name="?version"?\s+value="?([0-9.\-]+)"?'
# digest: 490a0046304402200a609ac520312f7f40892a75d8f1336d5a434c64900ae82272b3400ee348a7e6022016f20c0d8eebacb16f41848d4480b1238755fc89094c500dd785da8ca7c6f623:922c64590222798bb761d5b6d8e72950