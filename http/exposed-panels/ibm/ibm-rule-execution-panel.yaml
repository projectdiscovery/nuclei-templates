id: ibm-rule-execution-panel

info:
  name: Rule Execution Server - Panel Detection
  author: DhiyaneshDK
  severity: info
  description: |
    Rule Execution Server panel was detected.
  reference:
    - https://www.ibm.com/docs/en/odm/8.12.0?topic=overview-introducing-rule-execution-server
  metadata:
    verified: true
    shodan-query: title:"Rule Execution Server"
  tags: panel,ibm,login,detect,rule-execution-server

http:
  - method: GET
    path:
      - "{{BaseURL}}/res/login.jsf"

    matchers-condition: and
    matchers:
      - type: word
        part: body
        words:
          - "<title>Rule Execution Server</title>"

      - type: status
        status:
          - 200
