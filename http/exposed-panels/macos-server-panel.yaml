id: macos-server-panel

info:
  name: macOS Server Login - Panel
  author: DhiyaneshDk
  severity: info
  metadata:
    max-request: 1
    shodan-query: title:"macOS Server"
  tags: panel,login,macos-server

http:
  - method: GET
    path:
      - "{{BaseURL}}"

    matchers-condition: and
    matchers:
      - type: word
        words:
          - 'macOS Server'
          - 'apple-system'
        condition: and

      - type: status
        status:
          - 200
