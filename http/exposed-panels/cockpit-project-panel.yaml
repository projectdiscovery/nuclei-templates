id: cockpit-project-panel

info:
  name: Cockpit Project Login Panel - Detect
  author: righettod
  severity: info
  description: |
    Cockpit Project products was detected.
  reference:
    - https://github.com/cockpit-project/cockpit
    - https://cockpit-project.org/
  metadata:
    max-request: 1
    verified: true
    shodan-query: http.html:"cockpit/static/login.css"
  tags: panel,cockpit,login

http:
  - method: GET
    path:
      - "{{BaseURL}}/"

    matchers:
      - type: dsl
        dsl:
          - 'status_code == 200'
          - 'contains_all(to_lower(body), "cockpit/", "is_cockpit_client")'
        condition: and

    extractors:
      - type: regex
        part: body
        name: os
        group: 1
        regex:
          - '(?i)"PRETTY_NAME"\s*:\s*"(.*?)"'
        internal: true

      - type: regex
        part: body
        name: hostname
        group: 1
        regex:
          - '(?i)"hostname"\s*:\s*"(.*?)"'
        internal: true

      - type: dsl
        dsl:
          - '"OS: " + os + " | Hostname: " + hostname'
# digest: 4a0a00473045022100b9b1f0b5a44dfaf504e75089b18b68edea63f3b68938ef8df5929ed0d5f95ac902205138817910f77c7ce47ddc0793c4a58e9f2baa9b82c35babe4b92888ff803a14:922c64590222798bb761d5b6d8e72950