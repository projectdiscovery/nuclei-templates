id: csp-script-src-wildcard

info:
  name: Content-Security-Policy "script-src" Wildcard Detected
  author: pussycat0x
  severity: info
  description: |
    Detected a wildcard (*) within the script-src directive of the Content-Security-Policy. This allowed scripts to load from any origin, weakening the CSP and increasing XSS risk.
  reference:
    - https://developer.mozilla.org/en-US/docs/Web/HTTP/Headers/Content-Security-Policy/script-src
  metadata:
    max-request: 1
    verified: true
  tags: csp,misconfig,headers,vuln

http:
  - method: GET
    path:
      - "{{BaseURL}}"

    matchers-condition: and
    matchers:

      - type: word
        part: header
        words:
          - "Content-Security-Policy"
        case-insensitive: true

      - type: regex
        part: header
        regex:
          - "(?i)script-src[^;]*\\*[^;]*(?:;|$)"
# digest: 490a0046304402203fed021ae8d88bd5ce2bbba8924e94153417a391e944c22934adc246a343d6c402206accca1252c094fd541eab1b8479d53c7d85de84351054ccae532514703098bf:922c64590222798bb761d5b6d8e72950