id: openremote-detect

info:
  name: OpenRemote IoT Platform - Detection
  author: pussycat0x
  severity: info
  description: |
    Detects exposed OpenRemote IoT platform instances by checking login page titles, API endpoints, and specific OpenRemote UI artifacts. OpenRemote isan open-source IoT platform with a Keycloak-backed auth system.
  reference:
    - https://github.com/openremote/openremote
    - https://openremote.io
  classification:
    cvss-metrics: CVSS:3.1/AV:N/AC:L/PR:N/UI:N/S:U/C:N/I:N/A:N
    cvss-score: 0.0
    cwe-id: CWE-200
  metadata:
    max-request: 6
    verified: true
    shodan-query: 'http.title:"OpenRemote"'
    fofa-query: 'title="OpenRemote"'
  tags: openremote,iot,panel,detect

http:
  - method: GET
    path:
      - "{{BaseURL}}/"
      - "{{BaseURL}}/manager/"

    stop-at-first-match: true
    matchers-condition: or
    matchers:
      - type: word
        part: body
        words:
          - "OpenRemote"
          - "openremote"
        condition: or

      - type: status
        status:
          - 200

      - type: word
        part: header
        words:
          - "openremote"
        case-insensitive: true
