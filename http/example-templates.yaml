id: aviatrix-detect

info:
  name: Aviatrix Detect
  author: princechaddha
  severity: info
  tags: tech,aviatrix

http:
  - method: GET
    path:
      - "{{BaseURL}}"

    matchers-condition: and
    matchers:

      - type: word
        part: body
        words:
          - "<title>Testr</title>"
