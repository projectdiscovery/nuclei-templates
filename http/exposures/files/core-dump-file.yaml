id: core-dump-file

info:
  name: Exposed Core Dump File
  author: kazet
  severity: medium
  tags: exposure,files,core-dump
  metadata:
    max-request: 1

http:
  - method: GET
    path:
      - "{{BaseURL}}/core"

    matchers:
      - type: regex
        regex:
          - '^\x7fELF'
