id: ssh-sha1-hmac-algo

info:
  name: SSH SHA-1 HMAC Algorithms Enabled
  author: pussycat0x
  severity: info
  description: |
    The SSH server at the remote end is set up to allow the use of SHA-1 HMAC algorithms.
  reference:
    - https://forums.ivanti.com/s/article/How-to-disable-SSH-SHA-1-HMAC-algorithms?language=en_US
  metadata:
    verified: true
    shodan-query: product:"OpenSSH"
  tags: javascript,ssh,misconfig,network

javascript:
  - pre-condition: |
      isPortOpen(Host,Port);
    code: |
      let m = require("nuclei/ssh");
      let c = m.SSHClient();
      let response = c.ConnectSSHInfoMode(Host, Port);
      log(to_json(response));

    args:
      Host: "{{Host}}"
      Port: "22"

    matchers-condition: and
    matchers:
      - type: word
        words:
          - "server_to_client_macs"
          - "client_to_server_macs"
        condition: and

      - type: word
        words:
          - "hmac-sha1"
# digest: 4a0a00473045022034cfb0507f7406f9bad2c42eab751fa96e13055f71a58d9e290cdce91b264dde022100bd49a8b0423de06072df79b56952480e82621be8faa86b6462674d866740cf25:922c64590222798bb761d5b6d8e72950