id: obsolete-ssh-version

info:
  name: Obsolete and less secure SSH Version
  author: pussycat0x
  severity: info
  reference:
    - https://nmap.org/nsedoc/scripts/sshv1.html
  metadata:
    shodan-query: "port:22"
  tags: javascript,ssh,network

javascript:
  - pre-condition: |
      isPortOpen(Host,Port)
    code: |
      var m = require("nuclei/ssh");
      var c = m.SSHClient();
      var response = c.ConnectSSHInfoMode(Host, Port);
      to_json(response);

    args:
      Host: "{{Host}}"
      Port: "22"

    matchers:
      - type: dsl
        dsl:
          - "success == true"
          - "version == '1.0'"
        condition: and

    extractors:
      - type: json
        name: SSH Protocol Version
        json:
          - '.ServerID.ProtoVersion'

      - type: json
        internal: true
        name: version
        json:
          - '.ServerID.ProtoVersion'
# digest: 490a0046304402203618e0a8ee6b7aae1fc651f562a2d4cf1827a65cd9560abf6efba708ce533caf022046d960d9b70902fd203cd7e0cf4223cc71db381c6dc33656cd2936c1559d9454:922c64590222798bb761d5b6d8e72950