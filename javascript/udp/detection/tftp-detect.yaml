id: tftp-detect

info:
  name: TFTP Service - Detection
  author: pussycat0x
  severity: info
  description: |
    Detect TFTP Service.
  metadata:
    verified: true
    max-request: 1
    shodan-query: "port:69"
  tags: js,tftp,udp,network
javascript:
  - pre-condition: |
      isUDPPortOpen(Host,Port);
    code: |
      let packet = bytes.NewBuffer();
      let message = "1.txt"
      let data = message;
      packet.WriteString(data)
      let c = require("nuclei/net");
      let conn = c.Open('udp', `${Host}:${Port}`);
      conn.SendHex(packet.Hex());
      let resp = conn.RecvString(256);
      resp;

    args:
      Host: "{{Host}}"
      Port: 69

    matchers:
      - type: word
        words:
          - "Unknown transfer ID"
        case-insensitive: true
# digest: 490a00463044022057d9ad9e1d04bdb44806ea08cd2364ae3f192cc3b392b4279715b2b69c551b9b02203f013e4827202b39bf860c421f57d3a63cbe827f32004fde3c1074ce7c555f23:922c64590222798bb761d5b6d8e72950