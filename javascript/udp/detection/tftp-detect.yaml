id: tftp-detect

info:
  name: TFTP Service - Detection
  author: pussycat0x
  severity: info
  description: |
    Detect TFTP Service.
  metadata:
    verified: true
    max-request: 1
    shodan-query: "port:69"
  tags: js,tftp,udp,network
javascript:
  - code: |
      let packet = bytes.NewBuffer();
      let message = "1.txt"
      let data = message;
      packet.WriteString(data)
      let c = require("nuclei/net");
      let conn = c.Open('udp', `${Host}:${Port}`);
      conn.SendHex(packet.Hex());
      let resp = conn.RecvString(256);
      resp;

    args:
      Host: "{{Host}}"
      Port: 69

    matchers:
      - type: word
        words:
          - "Unknown transfer ID"
        case-insensitive: true
# digest: 4a0a0047304502207ed818c5dbfafa706e7a8ae3d3eae6bc22d810c9e70d3e911d7633c9e23667bc022100eeca5c8334770c6250a07ce1820feb9337f83a2cafe7be75faa580fa77682257:922c64590222798bb761d5b6d8e72950