id: cgi-test
info:
  name: cgi test page
  author: YASH ANAND @yashanand155
  severity: informative

requests:
  - method: GET
    path:
      - "{{BaseURL}}/cgi-bin/test/test.cgi"
    matchers:
      - type: word
        words:
          - "HTTP_ACCEPT"
