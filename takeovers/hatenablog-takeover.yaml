id: hatenablog-takeover

info:
  name: hatenablog takeover detection
  author: pdteam
  severity: info
  tags: takeover

requests:
  - method: GET
    path:
      - "{{BaseURL}}"

    matchers-condition: and
    matchers:
      - type: dsl
        dsl:
          - Host != ip

      - type: word
        words:
          - 404 Blog is not found
