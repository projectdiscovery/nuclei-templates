id: bicak-desa-id-exposure

info:
  name: Information Exposure at bicak.desa.id
  author: ProjectDiscoveryAI
  severity: medium
  description: |
    This template checks for potential information exposure at bicak.desa.id by attempting to access publicly accessible files or directories.
  remediation: |
    Ensure that sensitive files and directories are not publicly accessible. Implement proper access controls and use server-side security features to restrict unauthorized access.

http:
  - raw:
      - |
        GET / HTTP/1.1
        Host: bicak.desa.id
        Accept: */*

    matchers-condition: and
    matchers:
      - type: status
        status:
          - 200

      - type: word
        words:
          - "<title>Bicak Desa</title>"