id: gcp-service-account

info:
  name: Google (GCP) Service-account
  author: gaurang
  severity: low
  tags: keys,file

file:
  - extensions:
      - all
      - txt

    extractors:
      - type: regex
        regex:
          - "\"type\": \"service_account\""