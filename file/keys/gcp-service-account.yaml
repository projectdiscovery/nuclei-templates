id: gcp-service-account

info:
  name: Google (GCP) Service-account
  author: gaurang
  severity: low
  tags: token,file

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "\"type\": \"service_account\""