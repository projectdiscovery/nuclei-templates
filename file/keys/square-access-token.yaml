id: square-access-token

info:
  name: Square Access Token
  author: gaurang
  severity: high
  tags: token,file

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "sq0atp-[0-9A-Za-z\\-_]{22}"
