id: square-access-token

info:
  name: Square Accesss Token
  author: gaurang
  severity: high
  tags: keys,file

file:
  - extensions:
      - all
      - txt

    extractors:
      - type: regex
        regex:
          - "sq0atp-[0-9A-Za-z\\-_]{22}"