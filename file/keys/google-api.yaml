id: google-api-key

info:
  name: Google API key
  author: gaurang
  severity: info
  tags: keys,file

file:
  - extensions:
      - all
      - txt

    extractors:
      - type: regex
        regex:
          - "AIza[0-9A-Za-z\\-_]{35}"