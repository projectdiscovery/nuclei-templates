id: square-oauth-secret

info:
  name: Square OAuth Secret
  author: gaurang
  severity: high
  tags: keys,file

file:
  - extensions:
      - all
      - txt

    extractors:
      - type: regex
        regex:
          - "sq0csp-[0-9A-Za-z\\-_]{43}"