id: mapbox-token

info:
  name: Mapbox Token
  author: Devang-Solanki
  severity: medium
  tags: token,file,mapbox

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - 'sk\.eyJ1Ijoi\w+\.[\w-]*'