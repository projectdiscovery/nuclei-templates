id: discord-backup-code

info:
  name: Discord Backup Code
  author: geeknik
  severity: high
  tags: discord,code,file

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - '[0-9A-Za-z]{4}\-[0-9A-Za-z]{4}'
