id: twitter-secret

info:
  name: Twitter Secret
  author: gaurang,daffainfo
  severity: medium
  tags: token,file

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "[1-9][0-9]+-[0-9a-zA-Z]{40}"
