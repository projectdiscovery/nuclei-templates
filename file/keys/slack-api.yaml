id: slack-api

info:
  name: Slack API Key
  author: gaurang
  severity: high
  tags: token,file,slack

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "xox[baprs]-([0-9a-zA-Z]{10,48})?"
