id: github-refresh-token

info:
  name: Github Refresh Token
  author: tanq16
  severity: high
  tags: token,file,github

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "ghr_.{36}"
