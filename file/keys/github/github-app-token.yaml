id: github-app-token

info:
  name: Github App Token
  author: tanq16
  severity: medium
  tags: token,file,github

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - "gh[us]_.{36}"
