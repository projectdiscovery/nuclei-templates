id: gitlab-recovery-code

info:
  name: Github Recovery Code
  author: geeknik
  severity: high
  tags: gitlab,recovery,token,file

file:
  - extensions:
      - all

    extractors:
      - type: regex
        regex:
          - '[0-9A-Za-z]{16}'
