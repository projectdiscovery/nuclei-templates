id: alive-hosts-checker

info:
  name: Alive host checker
  author: "@pdnuclei"
  severity: info
  
  # accepts dns hostnames/ips as input
  
requests:
  - method: GET
    path:
      - "https://{{BaseURL}}"
    matchers:
      - type: dsl
        dsl:
        - "status_code>=200"
