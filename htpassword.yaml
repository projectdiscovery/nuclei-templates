id: htpassword 

info:
  name: htpassword file
  author: Yash Anand @yashanand155
  severity: info

requests:
  - method: GET
    path:
      - "{{BaseURL}}/.htpassword"
    matchers:
      - type: word
        words:
          - ":$"
